<Window x:Class="OnePass.WPF.Windows.SyncWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OnePass.WPF.Windows"
        xmlns:fa="http://schemas.fontawesome.com/icons/svg" 
        xmlns:models="clr-namespace:OnePass.WPF.Models" 
        d:DataContext="{d:DesignInstance Type=models:SyncModel}"
        mc:Ignorable="d"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner" 
        Title="OnePass Sync" ShowInTaskbar="False" WindowStyle="SingleBorderWindow"
        SizeToContent="WidthAndHeight" SourceInitialized="Window_SourceInitialized" Closing="Window_Closing">

    <StackPanel Margin="10,0,10,0" MinWidth="200px">

        <GroupBox Visibility="{Binding SyncDetailsVisibility }">

            <GroupBox.Header>
                Details
            </GroupBox.Header>

            <TextBlock Text="{Binding SyncDetails}" />

        </GroupBox>

        <Grid VerticalAlignment="Center" MinWidth="100px" Height="100px" Visibility="{Binding SpinnerVisibility}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <fa:ImageAwesome Icon="Solid_Spinner" Spin="True" SpinDuration="4" Width="20px" />
                <Label HorizontalContentAlignment="Center" Content="{Binding SyncStatus}" />
                <Label HorizontalContentAlignment="Center" Content="{Binding HostName}" />
            </StackPanel>
        </Grid>

        <Button Style="{StaticResource PrimaryButton}" Content="Start Sync" Margin="0,10" IsEnabled="{Binding StartSyncButtonEnabled}" Click="SyncButton_Click" />

    </StackPanel>

</Window>
