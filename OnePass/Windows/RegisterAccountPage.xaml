<Page x:Class="OnePass.Windows.RegisterAccountPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:OnePass.Windows"
      mc:Ignorable="d" 
      d:DesignHeight="400" d:DesignWidth="300"
      Title="RegisterAccountPage">

    <StackPanel Margin="10">

        <Label Content="Username" />
        <TextBox Text="Test" />

        <Label Content="Password" />
        <PasswordBox />

        <Label Content="Repeat Password" />
        <PasswordBox />

        <Grid Margin="0,10,0,5">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Create" IsDefault="True" />
            <Button Grid.Column="3" Content="Back" />

        </Grid>

    </StackPanel>

</Page>
