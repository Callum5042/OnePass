<Window x:Class="OnePass.WPF.Windows.ContentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OnePass.WPF.Windows" 
        mc:Ignorable="d" ResizeMode="CanResizeWithGrip"
        Title="OnePass" Height="450" Width="800">

    <DockPanel>

        <!-- Menu bar -->
        <Menu DockPanel.Dock="Top" Background="White">

            <MenuItem Header="File">
                <MenuItem Header="Exit" />
            </MenuItem>

        </Menu>

        <!-- Status bar -->
        <StatusBar DockPanel.Dock="Bottom" Background="#FFF0F0F0">

            <StatusBarItem>
                <TextBlock Text="" />
            </StatusBarItem>

        </StatusBar>

        <!-- Content -->
        <Grid Background="White">

            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <!-- Toolbar -->
            <!--<Border Grid.Row="0" Padding="10" BorderThickness="0,1,0,0" BorderBrush="gray" Background="#FFF0F0F0">

                <StackPanel Orientation="Horizontal">

                    <Button Style="{StaticResource PrimaryButton}" Content="Add" Margin="0,0,10,0" Padding="10" />
                    <Button Content="Sync" Margin="0,0,10,0" />
                    <Button Content="Options" Margin="0,0,10,0" />

                    <TextBox Width="200" />

                </StackPanel>

            </Border>-->

            <!-- Products -->
            <ListView Grid.Row="1" x:Name="ListView" ItemsSource="{Binding Path=ProductDetails}">

                <!-- Style -->
                <ListView.ItemContainerStyle>

                    <Style TargetType="ListViewItem">
                        <Setter Property="Height" Value="30" />
                        <Setter Property="Foreground" Value="Black" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <Border BorderBrush="Transparent" BorderThickness="0" Background="{TemplateBinding Background}">
                                        <GridViewRowPresenter HorizontalAlignment="Stretch" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Width="Auto" Margin="0" Content="{TemplateBinding Content}"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" Value="#FF1164B4" />
                                <Setter Property="Foreground" Value="WhiteSmoke" />
                                <Setter Property="BorderThickness" Value="0" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>

                <!-- Grid -->
                <ListView.View>
                    <GridView>

                        <GridView.ColumnHeaderContainerStyle>
                            <Style TargetType="GridViewColumnHeader">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="GridViewColumnHeader">
                                            <Grid>
                                                <Border Background="Red" BorderBrush="#FFD4D4D4" BorderThickness="0" Name="HeaderBorder" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter Name="HeaderContent" Content="{TemplateBinding Content}" Margin="0" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                                </Border>
                                                <Thumb x:Name="PART_HeaderGripper" HorizontalAlignment="Right" Margin="0,0,-9,0" Style="{StaticResource GridViewColumnHeaderGripper}" />
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsPressed" Value="true">
                                                    <Setter TargetName="HeaderContent" Property="Margin" Value="0,0,0,0"/>
                                                    <Setter Property="Background" Value="Blue"/>
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="false">
                                                    <Setter Property="Opacity" Value=".5" TargetName="HeaderBorder"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </GridView.ColumnHeaderContainerStyle>

                        <GridViewColumn DisplayMemberBinding="{Binding Path=Name}" Header="Name" Width="200" />

                    </GridView>
                </ListView.View>

            </ListView>

        </Grid>

    </DockPanel>

</Window>
