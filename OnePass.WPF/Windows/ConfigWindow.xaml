<Window x:Class="OnePass.WPF.Windows.ConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:OnePass.WPF.Models"
        d:DataContext="{d:DesignInstance Type=models:ConfigModel}"
        mc:Ignorable="d"
        Title="OnePass Configuration" SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False"
        SourceInitialized="Window_SourceInitialized">

    <!-- Resources -->
    <Window.Resources>

        <!-- TextBox style -->
        <Style TargetType="TextBox">
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="3" />
                </Style>
            </Style.Resources>
            <Setter Property="Padding" Value="2" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Validation.ErrorTemplate" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Opacity" TargetName="border" Value="0.56"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="true">
                                <Setter Property="BorderThickness" Value="1"></Setter>
                                <Setter Property="BorderBrush" Value="#FF1164B4"></Setter>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#FF0080FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="BorderBrush" Value="Red" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Label style -->
        <Style TargetType="Label">
            <Setter Property="Padding" Value="0" />
        </Style>

    </Window.Resources>

    <StackPanel Margin="10,0,10,0">

        <GroupBox>
            
            <GroupBox.Header>
                <Label>Password Generator</Label>
            </GroupBox.Header>

            <Grid Margin="5">
                
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="160" />
                </Grid.ColumnDefinitions>

                <CheckBox Content="Lowercase" Grid.Row="0" Grid.Column="0" IsChecked="{Binding Lowercase}" />
                <CheckBox Content="Uppercase" Grid.Row="0" Grid.Column="3" IsChecked="{Binding Uppercase}" />
                <CheckBox Content="Numbers" Grid.Row="1" Grid.Column="0" IsChecked="{Binding Numbers}" />
                <CheckBox Content="Symbols" Grid.Row="1" Grid.Column="3" IsChecked="{Binding Symbols}" />

                <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,5,0,0">
                    <Label Content="Min Length" />
                    <TextBox Text="{Binding MinLength, UpdateSourceTrigger=PropertyChanged}" TabIndex="1" />
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="2" Margin="0,5,0,0">
                    <Label Content="Max Length" />
                    <TextBox Text="{Binding MaxLength, UpdateSourceTrigger=PropertyChanged}" TabIndex="2" />
                </StackPanel>

            </Grid>
            
        </GroupBox>

        <GroupBox Margin="0,10,0,0">

            <GroupBox.Header>
                <Label>Password History</Label>
            </GroupBox.Header>

            <Grid Margin="5">

                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="160" />
                </Grid.ColumnDefinitions>

                <CheckBox Content="Enable Password History" Grid.Row="0" Grid.Column="0" IsChecked="{Binding EnablePasswordHistory}" />

            </Grid>

        </GroupBox>

        <Label Content="{Binding ValidationMessage}" Style="{StaticResource ValidationLabel}" />
        <Button Style="{StaticResource PrimaryButton}" Content="Save" Margin="0,10" Click="Button_Click_SaveConfig" />

    </StackPanel>

</Window>
